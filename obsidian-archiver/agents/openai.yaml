interface:
  display_name: "Obsidian Archiver (Legacy)"
  short_description: "Legacy one-step archive flow via x-reader."
  default_prompt: "Use $obsidian-archiver only when you need the legacy x-reader based one-step archival path for Obsidian."