# Phase 6 - iPhone 快捷指令提交即返回模式

关联总计划：[[v3.5 Gateway Async and Feishu Callback PRD]]

## 目标

把 iPhone 快捷指令从“等待最终结果”改成“提交即结束”，只显示 `request_id` 和提示文案。

## 本阶段应完成

- 快捷指令只提交任务
- 快捷指令不再轮询最终状态
- 快捷指令文案改为“结果将稍后通过飞书返回”
